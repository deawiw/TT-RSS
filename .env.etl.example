# Отдельные секреты для ETL-модуля TT-RSS.
# Этот файл не используется Docker Compose и не должен подменять корневой .env.
TTRSS_BASE_URL=http://localhost:8280
TTRSS_USER=news_etl
TTRSS_PASSWORD=change_me_etl_password
